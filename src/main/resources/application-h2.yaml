spring:
        datasource:
                     url: jdbc:h2:~/tmp/${db.name};DB_CLOSE_ON_EXIT=FALSE
                     username: sa
                     password:
                     driver-class-name: org.h2.Driver
        dbunit:
                data-factory: org.dbunit.ext.h2.H2DataTypeFactory
        jpa:
            show-sql: true