spring:
  jpa:
    open-in-view: true
    show-sql: false
    hibernate:
      ddl-auto: none
  datasource:
    max-wait: 10000
    max-active: 5
    test-on-borrow: true
    url: jdbc:h2:~/tmp/${db.name}
    username: sa
    password:
    driver-class-name: org.h2.Driver
  dbunit:
    operationType: CLEAN_INSERT
    sample-file: src/test/resources/sample-data.xml
    data-factory: org.dbunit.ext.h2.H2DataTypeFactory
  jackson:
    serialization:
      write_dates_as_timestamps: true
  security:
    user:
      name: changeme
      password: changeme
db:
  name: appsugar_kotlin
server:
  port: 80
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
shiro:
  cipher:
    key: u39fskcJooyWj2jA6Vs2lA==
  session:
    name: appsession

---
spring:
  profiles: mysql
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:~/tmp/${db.name}
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  dbunit:
    data-factory: org.hibernate.dialect.MySQL5InnoDBDialect