<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.hazelcast.com/schema/config
           http://www.hazelcast.com/schema/config/hazelcast-config-3.9.xsd">
    <instance-name>${spring.hazelcast.instance-name}</instance-name>
    <group>
        <name>${spring.hazelcast.group.name}</name>
        <password>${spring.hazelcast.group.password}</password>
    </group>
    <network>
        <port auto-increment="true" port-count="100">5701</port>
        <join>
            <multicast enabled="true"/>
        </join>
        <interfaces enabled="${spring.hazelcast.network.interfaces.enabled}">
            <interface>${spring.hazelcast.network.interfaces.interface}</interface>
        </interfaces>
    </network>
    <map name="shiro-activeSessionCache">
        <async-backup-count>1</async-backup-count>
        <time-to-live-seconds>1800</time-to-live-seconds>
    </map>
    <map name="org.appsugar.archetypes.web.security.ShiroRealm.authenticationCache">
        <async-backup-count>1</async-backup-count>
        <time-to-live-seconds>1800</time-to-live-seconds>
    </map>
    <properties>
        <property name="hazelcast.max.no.heartbeat.seconds">5</property>
    </properties>
</hazelcast>