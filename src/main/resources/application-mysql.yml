spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost/${db.name}?createDatabaseIfNotExist=true&useSSL=false&characterEncoding=utf-8&rewriteBatchedStatements=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver